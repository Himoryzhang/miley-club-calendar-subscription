{
  "name": "wechat-course-calendar-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure frontend tool for syncing WeChat mini-program course bookings into calendar apps.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "preview": "netlify dev",
    "start": "netlify dev"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "netlify-cli": "^26.0.1",
    "typescript": "^5.7.3"
  }
}
